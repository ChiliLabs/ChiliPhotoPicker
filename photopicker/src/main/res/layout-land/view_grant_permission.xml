<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/permissionTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:paddingStart="0dp"
        android:paddingEnd="32dp"
        android:text="@string/picker_media_permissions_hint"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:textColor="?attr/pickerPrimaryColor"
        app:layout_constraintEnd_toStartOf="@+id/grantTextView"
        app:layout_constraintHorizontal_chainStyle="packed"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/grantTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?selectableItemBackgroundBorderless"
        android:text="@string/picker_allow"
        android:textAppearance="@style/TextAppearance.MediaPicker.Button"
        app:layout_constraintBottom_toBottomOf="@+id/permissionTextView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/permissionTextView"
        app:layout_constraintTop_toTopOf="@+id/permissionTextView" />

</androidx.constraintlayout.widget.ConstraintLayout>